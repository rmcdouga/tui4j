package com.williamcallahan.tui4j.message;

/**
 * Represents modifier keys applied to Enter when reported via Kitty CSI-u.
 */
public enum EnterKeyModifier {
    /** Shift modifier. */
    Shift,
    /** Control modifier. */
    Ctrl,
    /** Control and Shift modifiers. */
    CtrlShift
}
